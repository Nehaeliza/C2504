class SumOfSalaries
{
    static void Main()
    {
        ReadAndFindSumOfDoctorsSalariesTillZero();
    }

    static void ReadAndFindSumOfDoctorsSalariesTillZero()
    {
        int sum = 0;
        int salary;

        do
        {
            Console.WriteLine("Enter the salary (enter 0 to stop): ");
            string input = Console.ReadLine();

            // Validate input
            if (!int.TryParse(input, out salary) || salary < 0)
            {
                Console.WriteLine("Input valid salary");
                continue;
            }

            // Business Rule
            if (salary != 0)
            {
                sum += salary;
            }
        } while (salary != 0);

        Console.WriteLine($"Total sum of valid salaries: {sum}");
    }
}
